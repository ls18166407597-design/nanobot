# 可用工具与高级组合 (Tools & Recipes)

直接访问并主动使用这些工具。

## 🛠️ 专家组合技 (Advanced Recipes)
*当你遇到复杂场景时，请优先考虑以下组合流程：*

### 1. 深度环境感知 (Environmental Insight)
**场景**: “这软件怎么用？”或“帮我看看这个窗口”。
**流程**: 
1. `mac_control(action="get_frontmost_app")`: 确认当前焦点。
2. `mac_control(action="activate_app", app_name="...")`: 强制置顶。
3. `mac_vision(action="look_at_screen", app_name="...")`: 进行原生视觉读取。
4. `peekaboo(cmd="see", app="...")`: 获取 UI 元素 ID 准备操作。

### 2. 闭环研究与报告 (Research & Synthesis)
**场景**: “帮我搜一下 X 的最新动态”。
**流程**:
1. `spawn(task="Search for X...", instructions="...")`: 委派子智能体。
2. `read_file(path=...)`: 如果涉及本地文档，先获取背景。
3. `write_file(path="/tmp/report...", content=...)`: 将子智能体结果写入临时文件进行整合。
4. 根据整合后的结论向老板汇报。

### 3. 系统级深度清理 (Deep Maintenance)
**场景**: “帮我清理一下系统”。
**流程**:
1. `exec(command="df -h")`: 检查磁盘。
2. `exec(command="du -sh ~/Downloads/*")`: 识别大文件。
3. `shell(command="...")`: 执行精细清理或移动操作。

---

## 📁 基础工具列表 (Base Tools)
*(详见各工具子文档)*

- **文件系统**: `read_file`, `write_file`, `edit_file`, `list_dir`
- **桌面控制**: `mac_control`, `mac_vision`, `peekaboo`
- **协作工具**: `github`, `gmail`
- **核心大脑**: `memory`, `knowledge` (RAG), `skills`
- **调度系统**: `spawn` (委派), `exec/shell` (底层执行)

