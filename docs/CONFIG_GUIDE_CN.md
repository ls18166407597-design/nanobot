# Nanobot 配置指南

本指南提供了配置 Nanobot 高级工具套件的详细说明。

## 🪄 魔法初始化 (推荐)

配置 Nanobot 最简单的方法是使用**魔法初始化**功能。只需启动智能体并直接发送您的凭据即可。

1. 启动智能体：
   ```bash
   nanobot agent
   ```
2. 发送如下消息：
   > "帮我设置 GitHub，Token 是 `your_token_here`；另外 Gmail 账号是 `your_email@gmail.com`，应用密码是 `xxxx-xxxx-xxxx-xxxx`。"

Nanobot 会自动分析这些凭据，进行验证，并为您保存配置。

---

## ⚙️ 手动配置

如果您更喜欢手动配置工具，可以编辑 `~/.nanobot/` 目录下的 JSON 文件。

### 📧 Gmail
- **文件**: `~/.nanobot/gmail_config.json`
- **字段**:
  - `email`: 您的 Gmail 地址。
  - `app_password`: 从 Google 账号设置中生成的 16 位应用密码。
  - `imap_server`: (可选) 默认为 `imap.gmail.com`。
  - `smtp_server`: (可选) 默认为 `smtp.gmail.com`。

### 🐙 GitHub
- **文件**: `~/.nanobot/github_config.json`
- **字段**:
  - `personal_access_token`: 具有 `repo` 和 `workflow` 权限的 GitHub PAT。
  - `username`: (可选) 您的 GitHub 用户名。

### 📚 知识库 (Obsidian)
- **文件**: `~/.nanobot/knowledge_config.json`
- **字段**:
  - `vault_path`: 您本地 Markdown/Obsidian 库的绝对路径。

### 🔍 Web 搜索 (Brave)
- **文件**: `~/.nanobot/web_config.json`
- **字段**:
  - `brave_api_key`: 您的 Brave Search API 密钥。

### 🧠 智能核心配置 (Brain)
- **文件**: `~/.nanobot/config.json` (位于 `brain` 字段下)
- **字段**:
  - `auto_summarize`: (布尔值) 是否开启自动对话总结。开启后可支持超长对话。默认 `true`。
  - `summary_threshold`: (整数) 触发总结的消息数量阈值。默认 `40`。
  - `safety_guard`: (布尔值) 是否开启 LLM 命令安全守卫。开启后 Shell 命令执行前会进行 AI 审核。默认 `true`。

---

## 🛡️ 安全提示
您的凭据**绝不会**上传至任何服务器。它们严格存储在您本地机器的 `~/.nanobot/` 目录下。为了增强安全性，您可以在主配置文件 `config.json` 中设置 `"restrictToWorkspace": true`。
